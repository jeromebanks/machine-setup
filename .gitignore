# Backups and logs — local only, never committed
backups/
logs/*.log

# Machine-local secrets
.zshrc.local

# macOS
.DS_Store
**/.DS_Store

# chezmoi local state (not the dotfiles source tree)
/.chezmoi.toml.tmpl
/.chezmoidata.yaml

# Editor
.vscode/
*.swp
*.swo
